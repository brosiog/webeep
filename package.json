{
  "name": "beeper-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "node --check src/*.js test/*.js"
  },
  "dependencies": {
    "@beeper/desktop-api": "^5.0.0"
  }
}