# Copy to /etc/beeper-web.env for the systemd service; do not commit the real file.
HOST=127.0.0.1
PORT=3000
BEEPER_ACCESS_TOKEN=
BEEPER_BASE_URL=http://127.0.0.1:23373
# Set only for local UI development without a Beeper Desktop bridge.
# BEEPER_MODE=mock